# If .env.testing exists, Laravel loads it instead of .env (see Laravel docs / testing).
# The test database name is defined only in phpunit.xml; the DB is created during the initial MySQL init (docker/mysql-init.sh).

APP_ENV=testing
APP_KEY=

DB_HOST=mysql
DB_DATABASE=app_test
DB_USERNAME=laravel
DB_PASSWORD=secret
