# При наличии .env.testing Laravel подгружает его вместо .env (см. доки Laravel / testing).
# Имя тестовой БД — только в phpunit.xml; база появляется при первом init MySQL (docker/mysql-init.sh).

APP_ENV=testing
APP_KEY=

DB_HOST=mysql
DB_DATABASE=app_test
DB_USERNAME=laravel
DB_PASSWORD=secret
